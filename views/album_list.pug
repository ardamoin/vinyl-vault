extends layout

block content
  h1= title
  
  ul 
    each album in album_list
        li
            a(href=album.url) #{album.title} by 
                each artist in album.artist
                    span #{artist.name}, 
    else 
        li There are no albums.

